import React from 'react';

const About = () => {
  return (
    <div>
      <h1>About</h1>
      <p>This weather app provides current weather information for any city. It allows users to enter a city name and fetch the latest weather details using an API. This app is built with React, using Axios for HTTP requests and React Router for navigation.</p>
    </div>
  );
};

export default About;
